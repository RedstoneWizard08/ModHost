export interface GameVersion {
    id: string;
    beta: boolean;
}

export interface ModLoader {
    id: string;
    name: string;
}

export interface Tag {
    id: string;
    name: string;
    icon: string;
}
